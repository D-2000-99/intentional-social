# Docker Compose: main backend network (external, created by main app's compose)
# Set to your main app's Docker network. Default is {project_dir}_default.
MAIN_BACKEND_NETWORK=social_100_default

# Database (shared with main backend). Use hostname "db" (Docker network).
# Production: intentional_social — Development: intentional_social_dev
DATABASE_URL=postgresql://postgres:postgres@db:5432/intentional_social

# Security (min 32 chars)
SECRET_KEY=your-secret-key-at-least-32-characters-long

# Moderators (comma-separated emails)
MODERATOR_EMAILS=moderator1@example.com,moderator2@example.com

# CORS allowed origins for observability frontend
OBSERVABILITY_CORS_ORIGINS=http://localhost:5174,http://localhost:3001

# Google OAuth (same as main backend)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8002/auth/google/callback

# R2/S3 storage (for user deletion). Same as main backend.
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_ENDPOINT_URL=
# R2_BUCKET_NAME=
